import { ReactMediaRecord } from 'react-media-record';


type Props = {
  handleStop: any;
};

function RecordMessage({ handleStop }: Props) {
  return <div>Record Message</div>;
}

export default RecordMessage;
